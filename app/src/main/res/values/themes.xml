<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MiniPlay" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="mainBackgroundColor">@color/white</item>
        <item name="mainTextColor">@color/black</item>
        <item name="secondTextColor">@color/grey</item>
        <item name="iconColor">@color/black</item>
        <item name="android:statusBarColor">?attr/mainBackgroundColor</item>
    </style>
</resources>